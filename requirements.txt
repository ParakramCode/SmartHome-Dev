# SmartHome platform — dependencies
# =========================================

# --- Messaging channels ---
python-telegram-bot==22.7      # Telegram dev/test channel

# --- LLM (hybrid parser fallback) ---
google-genai==2.8.0            # Google Gemini client

# --- HTTP / web ---
httpx==0.28.1                  # async client for Home Assistant + WhatsApp
fastapi==0.118.0              # webhook + admin API
uvicorn[standard]==0.34.0     # ASGI server
itsdangerous==2.2.0           # signed admin session cookies
python-multipart==0.0.20      # form parsing (admin login)

# --- Scheduling ---
APScheduler==3.11.0           # recurring/scheduled commands

# --- Config ---
python-dotenv==1.2.2          # .env loading
PyYAML==6.0.2                 # config.yaml parsing

# --- Voice notes (transcription) ---
# Whisper is heavy (pulls torch). Installed separately when the voice
# feature is enabled:  pip install openai-whisper

# --- Dev / testing ---
pytest==9.1.0
pytest-asyncio==1.4.0
